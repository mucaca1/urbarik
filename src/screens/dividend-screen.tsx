import { useTranslation } from "react-i18next";

export const DividendScreen: React.FC = () => {
        const { t } = useTranslation();
    return (
        <div>
            <h1>Dividend Screen</h1>
        </div>
    );
}